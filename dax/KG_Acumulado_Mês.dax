KG Acumulado Mês =


VAR acumulado = 
    SUM('Faturamento Mês SD2010 (DQ)'[D2_QUANT])

VAR devolucoes =
    SUM('Devoluções Mês (DQ)'[D1_QUANT])

VAR totalMes = acumulado - devolucoes

RETURN
    IF(totalMes <= 0, 0, totalMes)